import styled from "styled-components/macro";

const FilterMenuGroupLabel = styled.label`
  display: flex;
  align-items: center;
  gap: ${(props) => props.theme.mimirorg.spacing.l};
`;

export default FilterMenuGroupLabel;
