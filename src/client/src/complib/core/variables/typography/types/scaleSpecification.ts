export interface ScaleSpecification {
  font: string;
  size: string | number;
  weight: string | number;
  family: string;
  tracking: number;
  letterSpacing: string;
  lineHeight: string | number;
}