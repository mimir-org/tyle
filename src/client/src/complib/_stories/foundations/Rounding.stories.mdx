import { Box, Flexbox } from "../../layouts";
import { Text } from "../../text";
import { theme } from "../../core";

<Meta title="Foundations/Rounding" />

# Rounding

## Overview

Here you will find an overview of the different units available for rounding of element corners.

## Rounding levels

<Box
  display="flex"
  justifyContent={"space-between"}
  gap={theme.spacing.large}
  p={theme.spacing.xl}
  border={`2px solid ${theme.color.light.outline.base}`}
  borderRadius={theme.border.radius.medium}
>
  {Object.entries(theme.border.radius).map(([k, v]) => {
    return (
      <Flexbox key={v} flexDirection={"column"} gap={theme.spacing.medium}>
        <Box borderRadius={v} border="2px solid red" width="100px" height="100px" p={theme.spacing.small} />
        <Flexbox flexDirection="column" gap={theme.spacing.small}>
          <Text variant={"title-medium"}>theme.spacing.{k}</Text>
          <Text variant={"label-large"}>{v}</Text>
        </Flexbox>
      </Flexbox>
    );
  })}
</Box>
